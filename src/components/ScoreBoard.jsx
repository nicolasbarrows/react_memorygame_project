//count and display the "current score" and "best score" so far
import { Component } from "react";

class ScoreBoard extends Component {
  render() {
    return {
      /*what to display*/
    };
  }
}

export default ScoreBoard;
